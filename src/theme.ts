// src/theme.ts
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    typography: {
        fontFamily: "CustomFontM,CustomFontEB,CustomFontB,CustomFontSB,CustomFontR, sans-serif",
      },
});

export default theme;
